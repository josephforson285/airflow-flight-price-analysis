# Unpinned on purpose: the Airflow constraints file in the Dockerfile pins
# these, and pinning in two places is how you get unresolvable conflicts.

apache-airflow-providers-mysql
apache-airflow-providers-postgres
apache-airflow-providers-common-sql

pytest
